接下来，你需要重构这个代码库，把 2D phased array 的 signal model 以及 basic processing 都写完整。在已有 code 的基础上进行添加。要求: [radar_signal_model_basic_processing.md](knowledge_base/radar_signal_model_basic_processing.md) 参考这里面的信号模型，基本符号假设。修改 [processing.py](basic_radar_signal_processing/processing.py) 这里面的角度估计算法，可以 implement 更高级的 DOA 算法，比如 MUSIC， 然后就是 CFAR 这种检测算法， 要求是 CFAR 可以跑在 RD map 上， RA map 上， RAD map 上。还有就是在 [signal_model.py](radar_sim/signal_model.py) 中，在给定雷达体制后，在 RadarSimulator 中还要计算角度的分辨率，并加入到 radar_config_summary() 这个 method 里面。最后，要对每一个 processing, i.e. pulse compression, doppler processing, DOA 算法，写一个 test, 放在 @tests 这个文件夹里，测试就是把 RD 图， 3D az-el DOA results，RA 图这些画出来，然后看看这些图能否正确显示目标的位置和角度以及速度。然后是在这些图上跑 CFAR，画出相应的 CFAR 结果。